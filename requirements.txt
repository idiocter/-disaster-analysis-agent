# Installed via `pip` inside the conda-forge environment created from
# environment.yml (GDAL/rasterio/geopandas/etc. come from conda-forge, not
# here -- see environment.yml and plan.md's note on Mac install pain).

# --- Phase 1: core orchestration, viz, report ---
langgraph>=1.2  # pin floor to avoid resolving alongside a mismatched langgraph-checkpoint
openai>=1.40
pydantic>=2
pydantic-settings>=2
python-dotenv>=1.0
structlog>=24.1
typer>=0.12
folium>=0.17
matplotlib>=3.9
contextily>=1.6
jinja2>=3.1
# weasyprint installed via conda-forge (environment.yml), not here -- see
# the comment there for why

# --- Phase 2: real GEE integration ---
earthengine-api
google-auth

# --- Phase 3: PostGIS integration ---
sqlalchemy[asyncio]>=2.0
geoalchemy2
asyncpg
pgvector
rapidfuzz

# --- Phase 6: RAG ---
sentence-transformers  # or voyageai, per EMBEDDING_PROVIDER

# --- Dev/test ---
pytest>=8
pytest-asyncio>=0.24
pytest-mock>=3.14
ruff>=0.6
mypy>=1.11
